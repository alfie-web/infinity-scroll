// const { models, model, Schema } = require('mongoose')

// const schema = new Schema({
//    role: {
//       type: String     // user, author, moderator, admin ...
//    }
// })

// module.exports = models.Feed || model('Feed', schema)
